/**
 * Feature flags for the app. Used to hide/show features without removing code.
 * Set to true to re-enable; the app will show the corresponding menu entry and routes.
 */
export const CHAT_ROOM_VISIBLE = false;
